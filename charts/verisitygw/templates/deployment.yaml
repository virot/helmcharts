apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "versitygw.fullname" . }}
  labels:
    app: {{ include "versitygw.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "versitygw.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "versitygw.name" . }}
    spec:
      containers:
        - name: versitygw
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 7070
          env:
            {{- if .Values.secret.enabled }}
            - name: ROOT_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secret.name }}
                  key: {{ .Values.secret.accessKeyName }}
            - name: ROOT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secret.name }}
                  key: {{ .Values.secret.secretKeyName }}
            {{- else }}
            - name: ROOT_ACCESS_KEY
              value: {{ .Values.env.ROOT_ACCESS_KEY | quote }}
            - name: ROOT_SECRET_KEY
              value: {{ .Values.env.ROOT_SECRET_KEY | quote }}
            {{- end }}
            - name: VGW_BACKEND
              value: {{ .Values.env.VGW_BACKEND | quote }}
            - name: VGW_BACKEND_ARG
              value: {{ .Values.env.VGW_BACKEND_ARG | quote }}
            - name: VGW_REGION
              value: {{ .Values.env.VGW_REGION | quote }}
          volumeMounts:
            - name: data
              mountPath: {{ .Values.nfs.mountPath }}
          livenessProbe:
            httpGet:
              path: {{ .Values.healthProbes.liveness.path }}
              port: 7070
            initialDelaySeconds: {{ .Values.healthProbes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.healthProbes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.healthProbes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.healthProbes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.healthProbes.readiness.path }}
              port: 7070
            initialDelaySeconds: {{ .Values.healthProbes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.healthProbes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.healthProbes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.healthProbes.readiness.failureThreshold }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: data
          nfs:
            server: {{ .Values.nfs.server }}
            path: {{ .Values.nfs.path }}
